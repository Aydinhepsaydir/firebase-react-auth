import FirebaseContext, { withFirebase } from "./context";
import Firebase, { StyledFirebaseAuth } from "./firebase";

export default Firebase;
export { FirebaseContext, withFirebase, StyledFirebaseAuth };
