import FirebaseContext, { withFirebase } from "./context";
import Firebase, { StyledFirebaseAuth, firebase } from "./firebase";

export default Firebase;
export { FirebaseContext, withFirebase, StyledFirebaseAuth, firebase };
